v0.22.0 收尾: 静态验收脚本去掉过时的硬编码 APK 版本断言

verify-nono-ip.py 原先硬编码「apk.version 预期 0.21.2」（编码阶段的断言，当时 APK 尚未重出）。APK 随 v0.22.0 发版后，该断言必然误报。改为版本无关的自洽性检查：apk.version 与网页版一致 / 落后则告警（提示需出包）/ 领先则失败，并校验 apk.url 与 apk.version 指向同一版本。复跑 28/28 通过。
