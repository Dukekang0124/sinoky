L1149   attr     '')"></div>\n<div id="updatebar"></div>\n<div class="rd-pop" id="rd-pop" role="dialog" aria-label="Word details"></div>\n<div id="rd-mask"></div>'
L1196   attr     '';\n/* ============ Sinoky v0.1.0 (M0) — single-file PWA, zero dependencies ============ */\n/* v0.3.35 S3：所有 localStorage 读写必须走这两个包装。\n   背景：iO'
L1243   attr     ''Someone says 你好 — this is your chance to answer like a real person.'
L1924   attr     ''Dinners, drinks, invitations. The fun part \\u2014 where "干杯" matters more than grammar.'
L2021   attr     ''北京'
L2040   attr     ''上海'
L2054   attr     ''杭州'
L2068   attr     ''重庆'
L2082   attr     ''成都'
L2096   attr     ''南昌'
L2110   attr     ''长沙'
L2124   attr     ''新疆'
L2138   attr     ''深圳'
L2152   attr     ''广州'
L2166   attr     ''厦门'
L2180   attr     ''海口'
L2194   attr     ''青岛'
L2208   attr     ''西安'
L2222   attr     ''桂林'
L2236   attr     ''张家界'
L2250   attr     ''大理'
L2264   attr     ''中国香港'
L3708   assign   ''+APP_VERSION;\n    renderLangList();\n    applyI18n(document.body);\n  }catch(e){}\n}\n/* ===== v0.14.0 i18n：原文即 key 的运行时翻译层 =====\n   字典外置 langs/'
L3883   toast    ''));\n  }\n}\n\n/* v0.11.0 应用内更新：APK 壳版本检测 + 原生桥下载安装 */\nasync function getLocalApkVersionCode(){\n  try{\n    if(typeof Capacitor==='
L4050   assign   "" for Sinoky in the opened page, then tap update again');\n        txt.style.color = '';\n      } else if(m.indexOf('cancelled') >= 0){\n       "
L4287   assign   "">'+T(m[1])+'</div>'; } }\n  else { pose.src = 'assets/mascot/like.webp'; if(stEl) stEl.style.display='none'; }\n}\n\n/* 按当前视图挑一句适合练习的中文 */\nNONO."
L4360   assign   "">Microphone blocked. Allow it in your browser, then try again.</div>';\n  }\n}\nfunction nonoStop(){ try{ if(NONO_REC && NONO_REC.state !== 'in"
L4553   assign   ''; return; }\n\n  /* 三维：准确度(接口) / 完整度(音节命中) / 流利度(语速估算) */\n  var syll = d.perSyll || [];\n  var hit = syll.filter(function(x){ return x.user && '
L4632   assign   ''s practise together. Hear it, then say it — <b>先听，再说。</b>";\n  if(box){\n    box.style.display = '
L8107   assign   ''<p class="nt-b">Pick any button below and go say one line. <b>说一句就行。</b></p>'
