{
  "version": "0.3.5",
  "updated": "2026-09-01",
  "note": "Score: added [SCORE] console logs so the silent 'Scoring...' hang can be diagnosed. Also redeployed the standalone sinoky-score worker (was likely unreachable). If it still hangs, the next step is to merge scoring into the Pages Worker (Pages doesn't support npm imports, so a simplified in-Worker scoring would be needed)."
}
