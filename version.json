{
  "version": "0.3.3",
  "updated": "2026-09-01",
  "note": "Score: surfaced real ASR errors (previously swallowed) and added vConsole for mobile debugging. 'Did not catch that' now shows the actual whisper reason. MediaRecorder errors (common on WeChat WebView) are reported instead of silent fail. Recording <500B is detected and prompted as 'no sound captured'."
}
