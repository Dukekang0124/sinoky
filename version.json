{
  "version": "0.3.23",
  "updated": "2026-09-02",
  "note": "v0.3.23 (backend security patch, no frontend change): _worker.js /api/* now guarded by Origin allowlist (same-host auto-pass) + per-IP 60s/40req sliding-window rate limit. Closes abuse vector on /api/tts /api/asr (billable CF Workers AI) and /api/feedback POST (KV write)."
}
