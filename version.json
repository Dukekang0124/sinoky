{
  "version": "0.3.4",
  "updated": "2026-09-01",
  "note": "Score fix: target moved from fetch header to URL query string. Headers must be ISO-8859-1 (ASCII); sending '你好' as header value was throwing 'non ISO-8859-1 code point', caught and silently shown as 'Did not catch that' since v0.3.1. Worker now reads from query (header kept for compatibility)."
}
